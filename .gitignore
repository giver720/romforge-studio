node_modules
dist
node_modules/.tmp
src-tauri/target
src-tauri/gen/schemas
__pycache__/
.DS_Store

# Las herramientas de terceros se descargan al preparar una version.
# No se guardan en el repositorio: usa `npm run tools:fetch` y `npm run chdman`.
src-tauri/binaries/*
!src-tauri/binaries/LEEME.txt

# Clave privada de firma del actualizador: NUNCA debe subirse
*.key
.tauri/

# Los artefactos de cada version van a GitHub Releases, no al repositorio
release/
